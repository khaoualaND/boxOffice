spring:
  application:
    name: Gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

server:
  port: 8090

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka





